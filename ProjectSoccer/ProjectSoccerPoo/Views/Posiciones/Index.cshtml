@model IEnumerable<ProjectSoccerPoo.Models.Posicion>

@{
    ViewBag.Title = "Plantilla - Posiciones";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Listado de posiciones de campo</h2>

<p>
    @Html.ActionLink("Crear posición", "Create", null, new { @class = "btn btn-warning" })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pos_descripcion)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.pos_descripcion)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.pos_codigo }, new { @class = "btn btn-success" })
                @Html.ActionLink("Detalles", "Details", new { id = item.pos_codigo }, new { @class = "btn btn-primary" })
                @Html.ActionLink("Eliminar", "Delete", new { id = item.pos_codigo }, new { @class = "btn btn-danger" })
            </td>
        </tr>
    }

</table>
